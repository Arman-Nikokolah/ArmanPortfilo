import React from "react";
import { ReactLoading } from "react-loading";

const Example = () => {
  return <ReactLoading type={"bar"} color={"green"} width={200} height={200} />;
};

export default Example;
